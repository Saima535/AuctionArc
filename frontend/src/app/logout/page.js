import { LogoutCard } from "@/components/public/LogoutCard";

export const metadata = {
  title: "Logout | AuctionArc",
  description: "Sign out of AuctionArc.",
};

export default function LogoutPage() {
  return <LogoutCard />;
}
